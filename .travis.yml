language: php
php:
- 5.5
- 5.6
before_script:
  # - composer self-update
  - cd tests
  - composer install --prefer-source --no-interaction --dev
script: phpunit --configuration ./tests/script/phpunit_chrome_saucelabs.xml
notifications:
  email:
    recipients:
      - borgogelli@iubar.it
    on_success: change # default: change
    on_failure: always # default: always

